# CS2640 project env template.
# Copy to .env and fill in. The only thing we need for Agent SDK billing is the
# OAuth token (from ~/.claude/sensitive/oauth-tokens.md, wzhu account).
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-REPLACE_ME
